/** 
 * 1. Déclaration du type de la configuration Next.js 
 * On utilise JSDoc pour spécifier que la configuration suit le type NextConfig fourni par Next.js
 */
const nextConfig = {};  // Configuration vide pour Next.js (on peut y ajouter des options plus tard)

// 2. Exportation de la configuration par défaut
export default nextConfig;  // On exporte la configuration pour que Next.js l'utilise
