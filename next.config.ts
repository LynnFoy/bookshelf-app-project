// Importation du type NextConfig depuis le module Next.js
import type { NextConfig } from "next";  // On importe le type NextConfig pour définir la configuration de Next.js

// 1. Déclaration de la configuration de Next.js avec le type NextConfig
const nextConfig: NextConfig = {
  // Ici, on peut ajouter différentes options de configuration pour Next.js
  /* config options here */  // Par exemple : mode de développement, réécriture d'URL, etc.
};

// 2. Exportation de la configuration par défaut
export default nextConfig;  // On exporte la configuration pour qu'elle soit utilisée par Next.js
